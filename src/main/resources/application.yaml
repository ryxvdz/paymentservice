server:
  port: 8081

spring:
  application:
    name: servicepayment


  cloud:
    function:
      definition: fraudTransactionConsumer

    stream:
      kafka:
        binder:
          brokers: localhost:9092

    topics:
      transacao-aprovada: "transacao-aprovada-topic"
      transacao-reprovada: "transacao-aprovada-topic"

